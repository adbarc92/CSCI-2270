#include <iostream>

int main()
{
	int runTime = 0;
	int sumRunTime = 0;
	int numRuns = 500;
	for (int i = 0; i < numRuns; ++i)
	{
		// for different Ns
			// for each of the three implementations
				// starttime
				// enqueue and dequeue
				// endtime
				// calculate runtime: endtime-starttime
				// SumRunTime += runTime
				// * Write data to a file
					// Columns: Implementation Type, n, runTime
			// wipe data structure to clear memory for next implementation
	}
	// Calc avg runtime: sumRunTime = sumRunTime/i
}

// to create random data sets
// generate two random numbers
	// generate a name: aaaaaa
	// increase each time: aaaaab

// Need at least one chart